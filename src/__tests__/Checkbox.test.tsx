// import React from 'react';
// import { render, screen } from '@testing-library/react';
// import '@testing-library/jest-dom';
// import { SmlsCheckbox } from '..';

// describe('Button', () => {
//   test('renders button', async () => {
//     const valueLabel = 'Nice test check';

//     expect(screen.queryByText(valueLabel)).toBeNull();

//     render(<SmlsCheckbox id={valueLabel} label={valueLabel} />);

//     expect(screen.getByText(valueLabel)).toBeInTheDocument();
//   });
// });
